@if(Request.IsAuthenticated) {
    <text>Welcome <strong>@Html.ActionLink(User.Identity.Name, "Profiles", "User", new { userId = Membership.GetUser().ProviderUserKey }, new { })</strong>!
    [ @Html.ActionLink("Log Off", "LogOff", "Member") ]</text>
}
else {
    @:[ @Html.ActionLink("Log On", "LogOn", "Member") ]
    @:[ @Html.ActionLink("Register", "LogOn", "Member") ]
}
